Place in this directory any media files that you would like to be pre-installed.

Note that we would strongly advise against putting large files in this directory
as this will make the install package very large. It would be preferable to 
directly copy large media files onto the phone SD card.